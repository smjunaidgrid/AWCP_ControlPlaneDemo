# PydanticAI runtime agent — free, local (Ollama). Requires Python >= 3.10.
pydantic-ai>=0.0.14
fastapi>=0.95.0
uvicorn[standard]>=0.22.0
pydantic>=2.2.0
psutil>=5.9.0  # to self-register under the radar's own process id
ddgs>=6.0.0   # free web search (no API key) for current-info questions
